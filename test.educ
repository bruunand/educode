program Test
  method main()
    Collection<number> rangeTest = range(5, 10)
    rangeTest.addItem(500)

    number testa = 5 modulo 3.5
    debug("res:" + testa)

    string s = "test"
    s = s.toString().toString().toString().toString().toString().toString()

    if "test" equals s then
        debug("it works")
    end if

    foreach number c in rangeTest
        debug("val:" + c)
    end foreach
    number x = robot.getOwner().getCoordinates().getX()

    robot.say("out " + random(1, 20))
    number num = 0
    number test
    num = test = 2
    repeat while num less than 50
      Coordinates coords = robot.getCoordinates()
      robot.setWorldTime(1200)
      coords.setX(coords.getX() + random(-2, 15))
      robot.walkTo(coords)
      robot.wait(5000)
      num = num + 1
    end repeat
  end method
end program