program Test
    method main()
        debug("okz")
        bool a = true
        bool b = false

        repeat while 1 equals 0
            debug("should not be in compiled code")

        end repeat
        bool c
        c = true

        if a and b then
            debug("Should not happen")
        else if c then
            debug("Should happen")
        else
            debug("else")
        end if

        if true and false then
            debug("remove me")
        else if false or false then
            debug("remove mee")
        end if

        repeat while a
            a = false
            debug("Loop")
        end repeat
    end method
end program