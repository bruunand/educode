program Test
    on event stringMessageReceived(11) call onMessageReceived

    number a = 5, b, c = 10

    method main()
        Coordinates coords = (0, 5, 10)
        Coordinates coords2 = (1, 2, 3)
        debug((coords + coords2).toString())

        Coordinates newCoords = coords + coords2

        Collection<Coordinates> andS = new Collection<Coordinates>(coords, coords2, newCoords)
        debug(andS.toString().toString().toString().toString().split("1").toString())

        Collection<number> test = new Collection<number>(1, 2, 3)
        Collection<number> test2 = new Collection<number>(1, 2, 3, 4)
        bool res = test equals test2

        string str = "hej jeg hedder anders, hej hvad hedder du"
        Collection<string> tet = str.split(" ")
        Collection<Collection<string>> doubly = new Collection<Collection<string>>()
        foreach string theis in tet
            doubly.addItem(new Collection<string>(random(-5, 5).toString()))
            doubly.addItem(theis.toUpperCase().split("E"))
        end foreach
        debug("doubly:" + doubly)
        tet[0] = tet[0].toUpperCase()
        debug("res:" + tet[0])
        debug("res:" + tet)
        debug("res:" + (test equals test2))
        debug("res:" + (test2 equals test2))
        debug("res:" + (test equals test))
        setter()
        debug("rand:" + random(-5,5))
        debug("a:" + a)
        return
    end method

    method setter()
        a = 10
    end method

    method onMessageReceived(Entity sender, string message)
    end method
end program