program Test
    method main()
        Collection<string> hej = new Collection<string>("ok")
        foreach string s in hej
            debug(s)
        end foreach
        bool a = true
        bool b = false

        bool c
        c = true

        if a and b then
            debug("Should not happen")
        else if c then
            debug("Should happen")
        else
            debug("else")
        end if

        if true and false then
            debug("remove me")
        else if false or false then
            debug("remove mee")
        end if

        repeat while a
            a = false
            debug("Loop")
        end repeat
    end method
end program