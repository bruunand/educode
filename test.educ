using Math

program Test
    method main()
        bool jd = true and false
        number a = 10
        a += 2
        coordinates c
        c -= (0, 1, 2)
        debug("c:" + c)
        debug("n:" + ----------------------------------------------------------------c)
        a = --a
        bool baller = not not not not not not true
        debug("baller:" + baller)

        if not not not not not true then
            debug("hej")
        end if
        debug("a:" + a)
        debug("Test:" + round(5.23231))
        debug("Test:" + abs(-5.23231))
        debug("Test:" + round(5.23231))
        debug("Out:" + c.toString())
        string str = "to er " + 2
        string str1 = "coord:" + c
        str += str1
    end method
end program