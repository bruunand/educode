program Test
    method main()
        number a = 5
        a = 123
        debug("T:" + a)

        Collection<number> test = new Collection<number>(a, 5, 10)
        foreach number c in test
            debug("T:" + c)
        end foreach
    end method
end program