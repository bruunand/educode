using Dig

program Test
    number glob2
    method main()
        number a = 10
        a += 2
        coordinates c
        c -= (0, 1, 2)
        debug("Out:" + c.toString())
    end method
end program