import java.util.*;
import com.educode.runtime.*;
import com.educode.runtime.types.*;
import com.educode.helper.*;

public class Test extends ProgramBase
{
Double global = 3.000000;
public void main() throws InterruptedException
{
Boolean jd = false;
Double a = 10.000000;
a = (a + 2.000000);
Coordinates c = new Coordinates(0.000000, 0.000000, 0.000000);
c = c.add(new Coordinates(0.000000, 1.000000, 2.000000), true);
System.out.println(("c:" + c));
System.out.println(("n:" + c.negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate().negate()));
a = -(-(a));
Boolean baller = !(!(!(!(!(!(true))))));
System.out.println(("baller:" + baller));
System.out.println(("a:" + a));
System.out.println(("Test:" + round(5.232310)));
System.out.println(("Test:" + abs(-5.232310)));
System.out.println(("Test:" + round(5.232310)));
System.out.println(("Out:" + c.toString()));
String str = ("to er " + 2.000000);
String str1 = ("coord:" + c);
str = (str + str1);
}
public Double round(Double num) throws InterruptedException
{
return (num - (num % 1.000000));
}
public Double abs(Double num) throws InterruptedException
{
if((num < 0.000000))
{
return (-1.000000 * num);
}
else
{
return num;
}

}
public void test() throws InterruptedException
{
Double a = 0.000000;
}
}