program HelloWorld //Debug program
    method TestMethodCallReturn() returns number
        number numa = 3
        number numb = 2
        number PIK
        bool B = false

        talk("Hej med dig", 6, numa)

        if numa greater than 5 then
            Add(numa, numb)
        else if numa not equals 8 then
            lol(fuck)
        else if numb equals numa then
            numba = 7
            B = not B
        else
            simon(22)
        end if

        repeat while numa less than numb
            talk((string)"hehe")
        end repeat

        numa = (string) 5

        return Add(numa + (numa - numb) * numa, numb)
    end method

    method Add(number numOne, number numTwo) returns number
        return numOne+numTwo
    end method
end program