program niceprogram
    method niceMain()
       //i need all the shit
       number a, b, c, d
       number e = f = 5
       number g = 6, h = 7
       bool T, F
       bool TF = true
       string fed = "FED STRENG MAND!1234!"#¤%&()"
       string tomstreng
       string halvtom = ikketom = "Federe streng mand"

       c = viskalbrugeenmetode(a, e, h, T, TF, fed, halvtom)
    end method

    method viskalbrugeenmetode(number i, number j, number k, bool B1, bool B2, string S1, string S2) returns number
       B1 = true

       if B2 then
           B1 = false
       else if not B1 or j less than k
           i = 900
       else
           i = 2
           fed = "anderledes streng"
       end if
       repeat while j less than k
           j++
       end true

       return j
    end method
end program