using Dig

program Math
    method round(number num) returns number
        return num - (num modulo 1)
    end method

    method abs(number num) returns number
        if num less than 0 then
            return -1 * num
        else
            return num
        end if
    end method
end program